# Elements: Cairo から ThorVG + richtext への移行計画

## 概要

Elements GUI ライブラリの描画バックエンドを Cairo から ThorVG に完全移行する。
- **図形描画**: ThorVG (../thorvg) を直接使用
- **テキスト描画・計測・レイアウト**: richtext (../richtext) を使用 (内部で ThorVG + FreeType + minikin)
- canvas クラスの公開 API は維持 → element/*.cpp は変更不要

## アーキテクチャ

```
[Window pixel buffer (uint32_t[])]
        ↑
[tvg::SwCanvas] ← Shape を追加 (図形)
        ↑
[canvas wrapper] ← パスコマンド蓄積 → fill/stroke 時に Shape 生成
        ↑                   ↑ テキスト描画時は flush → richtext で描画
[element::draw()]    [richtext::TextRenderer / TextLayout]
```

### 図形描画フロー
1. パスコマンド (move_to, line_to, arc 等) を PathCommand/Point 配列に蓄積
2. fill()/stroke() 時に tvg::Shape を生成、変換・クリップ・スタイルを設定
3. tvg::SwCanvas に add()
4. フレーム終了時に update() + draw() + sync()

### テキスト描画フロー
1. fill_text()/stroke_text() 時に蓄積中の図形を flush (update+draw+sync)
2. richtext::TextRenderer でピクセルバッファに直接描画
3. ThorVG Canvas をリセットして次のバッチへ

### テキスト計測
- richtext::TextLayout でグリフ位置・幅・フォントメトリクスを取得
- scratch canvas 不要

### glyphs (複雑テキストレイアウト)
- richtext::TextLayout → GlyphInfo 配列 (各文字の位置・幅)
- richtext::ParagraphLayout → 行分割
- richtext::TextRenderer → 描画

## 対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `canvas.hpp` | cairo_t → ThorVG/richtext 内部構造 |
| `canvas.cpp` | 全描画操作を ThorVG + richtext で再実装 |
| `canvas_impl.hpp` | インライン更新 |
| `scratch_context.hpp` | 削除 (richtext で計測) |
| `pixmap.hpp/cpp` | cairo_surface_t → uint32_t* ピクセルバッファ |
| `font.hpp/cpp` | cairo_font_face_t → 手動フォント登録 + richtext::FontManager |
| `glyphs.hpp/cpp` | Cairo glyph → richtext::TextLayout/ParagraphLayout |
| `base_view.hpp` | draw(cairo_t*) → draw(uint32_t*, uint32_t, uint32_t) |
| `view.hpp/cpp` | 描画フロー更新 |
| `host/windows/base_view.cpp` | DIBSection → pixel buffer → BitBlt |
| `CMakeLists.txt` | Cairo → ThorVG + richtext |
