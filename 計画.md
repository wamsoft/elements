# Elements: Cairo から ThorVG への移行計画

## 概要

Elements GUI ライブラリの描画バックエンドを Cairo から ThorVG に完全移行する。
テキスト描画・グリフレイアウトはプラグイン構造で差し替え可能。

- **図形描画**: ThorVG を直接使用
- **テキスト描画**: プラグイン（デフォルト: ThorVG `tvg::Text`、オプション: richtext `GlyphRenderer`）
- **グリフレイアウト**: プラグイン（デフォルト: FreeType + HarfBuzz 直接、オプション: richtext/minikin）
- **ホスト層**: Win32（デフォルト）または SDL3
- canvas クラスの公開 API は維持 → element/*.cpp は変更不要

## アーキテクチャ

```
[Window pixel buffer (uint32_t[])]
        ↑
[tvg::SwCanvas] ← Shape を追加 (図形)
        ↑
[canvas wrapper] ← パスコマンド蓄積 → fill/stroke 時に Shape 生成
        ↑                   ↑ テキスト描画は text_backend プラグイン経由
[element::draw()]    [text_backend (ThorVG or richtext)]
                            ↑
                     [glyph_layout_backend (FT+HB or richtext)]
```

### 図形描画フロー
1. パスコマンド (move_to, line_to, arc 等) を PathCommand/Point 配列に蓄積
2. fill()/stroke() 時に tvg::Shape を生成、変換・クリップ・スタイルを設定
3. tvg::SwCanvas に add()
4. フレーム終了時に update() + draw() + sync()

### テキスト描画フロー
1. fill_text()/stroke_text() → text_backend インターフェース経由
2. ThorVG バックエンド: tvg::Text でレンダリング
3. richtext バックエンド: GlyphRenderer でグリフ単位ベクター描画
4. いずれも tvg::SwCanvas 上に描画

### テキスト計測・グリフレイアウト
- glyph_layout_backend インターフェース経由
- FT+HB バックエンド: hb_shape() + FT_Face メトリクス（デフォルト）
- richtext バックエンド: TextLayout/minikin（オプション）

## ビルド構成

| オプション | デフォルト | 説明 |
|-----------|-----------|------|
| `ELEMENTS_HOST_UI_LIBRARY` | `win32` | ホスト: `win32` or `sdl` |
| `ELEMENTS_USE_RICHTEXT` | `OFF` | richtext バックエンド有効化 |

### ELEMENTS_USE_RICHTEXT=OFF（デフォルト）
- ThorVG: FetchContent (wtnbgo/thorvg cmake ブランチ)
- FreeType: vcpkg
- HarfBuzz 13.1.1: FetchContent (ICU なし)

### ELEMENTS_USE_RICHTEXT=ON
- richtext: FetchContent (wamsoft/richtext) → ThorVG + minikin + HarfBuzz + FreeType 含む

## 対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `canvas.hpp/cpp` | cairo_t → ThorVG SwCanvas、text_backend プラグイン |
| `canvas_impl.hpp` | インライン更新 |
| `scratch_context.hpp` | 4x4 ThorVG SwCanvas に簡素化 |
| `pixmap.hpp/cpp` | cairo_surface_t → tvg::Picture |
| `font.hpp/cpp` | cairo_font_face_t → 手動登録 + font_backend プラグイン |
| `glyphs.hpp/cpp` | Cairo glyph → glyph_layout_backend プラグイン |
| `text_backend.hpp` | テキスト描画プラグインインターフェース |
| `text_backend_tvg.cpp` | ThorVG テキストバックエンド（デフォルト） |
| `text_backend_richtext.cpp` | richtext テキストバックエンド（オプション） |
| `glyph_utils.hpp` | グリフレイアウトプラグインインターフェース |
| `glyph_layout_ft.cpp` | FreeType+HarfBuzz グリフバックエンド（デフォルト） |
| `glyph_layout_richtext.cpp` | richtext グリフバックエンド（オプション） |
| `base_view.hpp` | draw(cairo_t*) → draw(canvas&)、Win32/SDL 型抽象化 |
| `host/windows/` | DIBSection → pixel buffer → BitBlt |
| `host/sdl/` | SDL3 ホスト層（SDL_Texture 経由描画） |
| `CMakeLists.txt` | Cairo → ThorVG + FT + HB、ELEMENTS_USE_RICHTEXT オプション |
