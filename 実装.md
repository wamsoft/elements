# Elements: Cairo → ThorVG 移行 - 実装状況

## 進捗サマリー

| フェーズ | 状態 | 完了日 |
|---------|------|--------|
| 1. 基盤層 (canvas コア) | 完了 | 2026-04-01 |
| 2. 画像・ピクセルマップ | 完了 | 2026-04-01 |
| 3. フォント・テキスト | 完了 | 2026-04-01 |
| 4. プラットフォーム統合 (Win32) | 完了 | 2026-04-01 |
| 5. ビルドシステム | 完了 | 2026-04-01 |
| 6. 検証・修正 | 完了 | 2026-04-04 |
| 7. テキスト描画プラグイン化 | 完了 | 2026-04-04 |
| 8. グリフレイアウトプラグイン化 | 完了 | 2026-04-04 |
| 9. SDL3 ホスト追加 | 完了 | 2026-04-04 |
| 10. richtext 非依存ビルド構成 | 完了 | 2026-04-04 |

## 詳細ステータス

### フェーズ 1: 基盤層

| ファイル | 状態 | メモ |
|---------|------|------|
| canvas.hpp | 完了 | ThorVG SwCanvas + パスコマンド蓄積方式、text_backend プラグイン対応 |
| canvas.cpp | 完了 | 全描画操作をThorVG Shapeで再実装、テキストは backend 経由 |
| canvas_impl.hpp | 完了 | get_state() アクセサ追加 |
| scratch_context.hpp | 完了 | 4x4 ThorVG SwCanvas に簡素化 |

### フェーズ 2: 画像

| ファイル | 状態 | メモ |
|---------|------|------|
| pixmap.hpp | 完了 | tvg::Picture ベース |
| pixmap.cpp | 完了 | PNG=ThorVG Picture、WebP=libwebp、他=stb_image。src rect クロッピング対応済み |

### フェーズ 3: フォント・テキスト

| ファイル | 状態 | メモ |
|---------|------|------|
| font.hpp | 完了 | register_font() API、load_fonts_from_directory() |
| font.cpp | 完了 | fontconfig 除去、font_backend プラグイン経由で登録 |
| glyphs.hpp | 完了 | char_pos を glyph_utils.hpp に分離 |
| glyphs.cpp | 完了 | glyph_layout_backend プラグイン経由（richtext 直接依存なし） |

### フェーズ 4-5: プラットフォーム・ビルド

| ファイル | 状態 | メモ |
|---------|------|------|
| host/windows/ | 完了 | DIBSection + pixel buffer + BitBlt |
| host/sdl/ | 完了 | SDL3 ホスト（SDL_Texture、DPI scale 対応） |
| base_view.hpp | 完了 | Win32/SDL 型抽象化（`#ifdef`） |
| CMakeLists.txt | 完了 | ELEMENTS_USE_RICHTEXT + ELEMENTS_HOST_UI_LIBRARY |

### フェーズ 6: 検証・修正 (2026-04-04)

| 修正内容 | メモ |
|---------|------|
| Sprite 描画 | canvas::draw(pixmap, src, dest) の src rect クロッピング実装 |
| テキスト色 | RGB クランプ（level() > 1.0 対応）+ alpha を Paint::opacity() で適用 |
| グラデーション | 二重変換バグ修正 + 1回塗り最適化 |
| クリップ合成 | clip() で既存クリップと intersection（Cairo の cairo_clip() 相当） |
| pixmap クリップ | draw(pixmap) で state clip と dest clip を交差 |
| テキストクリップ | fill_text/stroke_text に make_clip_shape() 追加 |
| clip_extent() | invert(_state.matrix) で正しくユーザー座標に変換 |
| TextBox 表示 | glyphs::draw() で top alignment + richtext ascent offset |
| TextInput | 空テキストでもメトリクス計算、プレースホルダー top alignment |
| リソースパス | CWD → 実行ファイルディレクトリ基準に変更 |

### フェーズ 7: テキスト描画プラグイン化 (2026-04-04)

| ファイル | メモ |
|---------|------|
| text_backend.hpp | 抽象インターフェース（仮想デストラクタ付き） |
| text_backend_tvg.cpp | ThorVG tvg::Text 実装（デフォルト） |
| text_backend_richtext.cpp | richtext GlyphRenderer 実装（ELEMENTS_USE_RICHTEXT=ON 時） |

### フェーズ 8: グリフレイアウトプラグイン化 (2026-04-04)

| ファイル | メモ |
|---------|------|
| glyph_utils.hpp | glyph_layout_backend + font_backend インターフェース |
| glyph_layout_ft.cpp | FreeType + HarfBuzz 直接実装（デフォルト） |
| glyph_layout_richtext.cpp | richtext TextLayout/FontManager 実装（ELEMENTS_USE_RICHTEXT=ON 時） |

### フェーズ 9: SDL3 ホスト (2026-04-04)

| ファイル | メモ |
|---------|------|
| host/sdl/app.cpp | SDL_Init、ThorVG init、フォント読み込み、イベントループ + poll |
| host/sdl/base_view.cpp | SDL_Texture 描画、マウス/キー/テキスト入力、クリップボード |
| host/sdl/window.cpp | SDL_Window 管理、DPI scale 対応 limits |

### フェーズ 10: richtext 非依存ビルド構成 (2026-04-04)

- ext/richtext サブモジュール廃止
- ELEMENTS_USE_RICHTEXT=OFF（デフォルト）: ThorVG + FreeType(vcpkg) + HarfBuzz(FetchContent)
- ELEMENTS_USE_RICHTEXT=ON: richtext を FetchContent（ThorVG + minikin + HarfBuzz + FreeType 含む）

## フォント登録方式

fontconfig を完全除去し、手動フォント登録方式に変更。

### API

```cpp
void register_font(
   std::string const& family,
   std::string const& file,
   font_constants::weight_enum  weight  = font_constants::weight_normal,
   font_constants::slant_enum   slant   = font_constants::slant_normal,
   font_constants::stretch_enum stretch = font_constants::stretch_normal
);

void load_fonts_from_directory(std::string const& dir);
```

### 内部動作

1. `register_font()` → 内部マップ (family → vector<font_entry>) に登録
2. 同時に `font_backend::register_font()` でバックエンドにも登録
3. `font::font(font_descr)` → `match()` で内部マップから weight/slant/stretch の最近傍を検索

## 残作業

- ARGB8888 ↔ BGRA 変換の最適化
- glyphs::for_each() の O(n²) 改善
- システムフォント検索（Windows Segoe UI 等）
- SDL3 ホストの細部調整（ドラッグ＆ドロップ等）
