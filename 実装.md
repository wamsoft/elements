# Elements: Cairo → ThorVG 移行 - 実装状況

## 進捗サマリー

| フェーズ | 状態 | 完了日 |
|---------|------|--------|
| 1. 基盤層 (canvas コア) | 完了 | 2026-04-01 |
| 2. 画像・ピクセルマップ | 完了 | 2026-04-01 |
| 3. フォント・テキスト | 完了 | 2026-04-01 |
| 4. プラットフォーム統合 | 完了 | 2026-04-01 |
| 5. ビルドシステム | 完了 | 2026-04-01 |
| 6. 検証・修正 | 未着手 | - |

## 詳細ステータス

### フェーズ 1: 基盤層

| ファイル | 状態 | メモ |
|---------|------|------|
| canvas.hpp | 完了 | ThorVG SwCanvas + パスコマンド蓄積方式に書き換え |
| canvas.cpp | 完了 | 全描画操作をThorVG Shape/Textで再実装、arc近似含む |
| canvas_impl.hpp | 完了 | スタイル適用をno-opに、tvg_canvas()アクセサ追加 |
| scratch_context.hpp | 完了 | 4x4 ThorVG SwCanvasに簡素化 |

### フェーズ 2: 画像

| ファイル | 状態 | メモ |
|---------|------|------|
| pixmap.hpp | 完了 | vector<uint32_t>バッファ方式に変更、data()/stride()追加 |
| pixmap.cpp | 完了 | PNG=ThorVG Picture、WebP=libwebp、他=stb_image |

### フェーズ 3: フォント・テキスト

| ファイル | 状態 | メモ |
|---------|------|------|
| font.hpp | 完了 | cairo_font_face_t* → string _family + string _file、register_font() API追加 |
| font.cpp | 完了 | fontconfig除去、手動フォント登録方式、richtext::FontManager連携 |
| glyphs.hpp | 完了 | cairo glyph/cluster → char_pos (x,advance,num_bytes) |
| glyphs.cpp | 完了 | richtext::TextLayoutで文字位置取得、手動改行 |

### フェーズ 4: プラットフォーム統合

| ファイル | 状態 | メモ |
|---------|------|------|
| base_view.hpp | 完了 | draw(cairo_t*) → draw(canvas&) |
| view.hpp | 完了 | draw override更新 |
| view.cpp | 完了 | cairo_recording_surface → scratch_context、draw(canvas&) |
| host/windows/base_view.cpp | 完了 | pixel buffer + SetDIBitsToDevice方式 (cairo_win32_surface削除) |

### フェーズ 5: ビルドシステム

| ファイル | 状態 | メモ |
|---------|------|------|
| CMakeLists.txt | 完了 | cairo/freetype2/fontconfig削除、richtext_lib (thorvg+minikin) 追加 |
| vcpkg.json | 完了 | cairo削除、libwebp+freetype(richtext用) のみ |

## フォント登録方式

fontconfig を完全除去し、手動フォント登録方式に変更。

### API

```cpp
// font.hpp で宣言
void register_font(
   std::string const& family,
   std::string const& file,
   font_constants::weight_enum  weight  = font_constants::weight_normal,
   font_constants::slant_enum   slant   = font_constants::slant_normal,
   font_constants::stretch_enum stretch = font_constants::stretch_normal
);
```

### 使用例

```cpp
// アプリ起動時に使用するフォントを全て登録
elements::register_font("Noto Sans", "resources/NotoSans-Regular.ttf");
elements::register_font("Noto Sans", "resources/NotoSans-Bold.ttf",
   font_constants::bold);
elements::register_font("Noto Sans", "resources/NotoSans-Italic.ttf",
   font_constants::weight_normal, font_constants::italic);

// 以降は従来通り font_descr で使用
auto f = font_descr{"Noto Sans"}.bold().size(16);
```

### 内部動作

1. `register_font()` → 内部マップ (family → vector<font_entry>) に登録
2. 同時に `richtext::FontManager::registerFont()` でファイルパスを登録
3. `font::font(font_descr)` → `match()` で内部マップから weight/slant/stretch の最近傍を検索
4. 将来的にはフォント検索処理を richtext 側に移管予定

### 除去した依存

- `fontconfig` ライブラリ (pkg-config, vcpkg 共に除去)
- `font_paths()` 関数 (フォントディレクトリ指定の仕組み)
- `get_user_fonts_directory()` 関数 (macOS 用)

## 残作業

- コンパイル・リンク検証 (フェーズ6)
- ARGB8888 ↔ BGRA変換の最適化 (SIMD等)
- richtext TextRendererの直接利用によるテキスト描画品質向上
- フォント検索処理の richtext 側への移管
- SDL ホスト統一 (Windows含む全プラットフォーム対応予定)

## 変更ログ

### 2026-04-01 (3)
- GTK3 ホスト実装を削除 (lib/host/gtk3/ ディレクトリごと削除)
- macOS ホスト実装を削除 (lib/host/macos/ ディレクトリごと削除)
- 最終的にSDLで全プラットフォーム統一予定、当面はWindows(Win32)のみテスト用に残す
- lib/CMakeLists.txt: ELEMENTS_HOST を Windows のみに簡素化、APPLE/GTK関連ビルド設定全削除
- base_view.hpp: プラットフォーム #ifdef 除去、Windows (HWND) のみに統一
- app.hpp: GTK/Cocoa 前方宣言・メンバ除去
- CMakePresets.json: Linux/macOS/SDL プリセット削除、Windows プリセットのみ
- cmake/ElementsConfigCommon.cmake: プラットフォーム自動検出除去、win32 固定
- root CMakeLists.txt: ELEMENTS_HOST_ONLY_WIN7 オプション削除

### 2026-04-01 (2)
- fontconfig 完全除去
- 手動フォント登録方式に変更 (register_font() API)
- font.hpp: font_paths()/get_user_fonts_directory() 削除、register_font() 追加
- font.cpp: fontconfig コード全削除、手動登録マップベースの match() に簡素化
- CMakeLists.txt: fontconfig の pkg_check_modules 削除
- vcpkg.json: fontconfig 削除 (ユーザー側で対応済み)

### 2026-04-01 (1)
- フェーズ1〜5の全ファイルを実装完了
- Cairo依存を完全除去 (GTK3ホスト以外)
- ThorVG + richtext(minikin) ベースの描画・テキスト基盤に移行
